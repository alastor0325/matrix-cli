#!/usr/bin/env python3
# Backwards-compatibility shim — delegates to matrix-cli notify
import os, sys
script = os.path.join(os.path.dirname(os.path.abspath(__file__)), "matrix-cli")
os.execv(sys.executable, [sys.executable, script, "notify"] + sys.argv[1:])
